# XP-T-005｜刘澜《学习之美》接入与验证记录

日期：2026-09-10。分支：`feat/learning-beauty-20260910`。审阅入口：[PR #4](https://github.com/daffnjk/xia-peng-method-skills/pull/4)。

## 来源与范围

书籍作者是刘澜，夏鹏课程仅提供本项目的场景型 Skill 4+1 封装结构；XP-T 是来源命名空间，不是作者署名。题名页日期为2020-12-03，不据此推定正式出版日期。

本次对实际上传文件重新检查：PDF为202个跨页图像，SHA-256为 `0bbd0f3da5f19ab3807c4e0c7eccead977fddc341d9bf1df6834705091f24a53`。前言、五章与后记共整理24组非连续选段，不是全书逐字稿，不声称覆盖全部案例与论证。印刷页、PDF跨页与项目选段锚点是三套不同定位；详见 `01_source/XP-T-005_provenance.json`。

继续验证时直接视觉抽查PDF跨页1、20、104、113、115、150、173、182、183、184、190，涵盖题名、公式、二维读书法、模式、四问、抽象阶梯、实际应用、迁移及后记。未使用OCR，不采纳手写批注，不声称独立人工语义审核或所引研究的实证验证。

原始PDF不上传仓库。已恢复的两份阅读记录保持字节不变：raw/reviewed的SHA-256均为 `7a5c774752083e1e053ba1fdcd5ffff3d2e2419bae30f8f43eed4367247b8a1d`。它们是首次视觉记录的同版副本，reviewed目录名不表示已经有人类独立校对。原有四个课程来源及其锁也不修改。

## 接入资产与差异

| 资产 | 内容与状态 |
| --- | --- |
| `01_source/raw/XP-T-005_selected_excerpts.txt` | 首次选段阅读记录，只读保留 |
| `01_source/reviewed/XP-T-005_reviewed.txt` | P001—P024项目锚点，不是原书自然段 |
| `01_source/XP-T-005_provenance.json` | 原文件身份、抽查范围及24组页码/知识卡映射 |
| `02_knowledge/principle_cards.yaml` | 新增XP-P-049—072，共24条；保留作者与工程化步骤标识 |
| `03_agent/METHOD_POLICY.md`及router | 增加外部书籍归属、范围、引用与应用状态边界 |
| `06_evals/learning_beauty_evals.jsonl` | 新增XP-E-075—098，共24条评测定义，不是通过记录 |
| `08_ops/skill_candidates/book-learning-transfer/` | 0.1.1候选流程；未注册、未自动启用 |
| `tests/test_learning_beauty.py` | 8项资产结构、引用、打包隔离与夹具回归检查 |

恢复后的项目共有5个登记来源、72条原则与7个已注册方法论Skill；补题后题库为98条。24条新题与24张卡的对应关系只证明覆盖索引，不证明每条子主张都已完成语义验收。模型定义保存在相关知识卡的 `model_definition` 字段，不新增第二套独立维护源。

同源关联保留但不合并作者署名。例如反学习与限制性短板、模式化学习与场景Skill、学习迁移与职业试炼可互相补充；操作模板、停止条件和状态管理是工程化应用，不写成作者逐字指令。不用书中公式计算学习力分数或成功率。

## 已处理的问题

第一次恢复提交 `44aa9ed35454b1b9775d0294f93f4090fa5e2a88` 后，CI运行 `34477767996` 的资产校验通过，但79项单元测试中27项报错：测试夹具复制了生产注册表，却把所有Skill版本固定为0.3.0，同时只创建四个合成来源。

修复提交 `83eefb7fffea3969e0f79061dbf34547322682e3` 使用独立的 `tests/fixtures/skill_registry.json` 与合成公共规则，Skill文本版本取夹具字段，不再跟随生产来源变化。没有降级生产版本、删除测试、放宽来源解析或移除授权门禁。该提交的CI运行 `34479011240` 已成功。

本次还补齐了原来缺失的来源追溯JSON与评测文件；候选Skill改为引用唯一正式题库路径，避免双份维护。明确四问第三问指调整所学内容以便应用，不是只写自己成为哪类人。对原书“更深体现抽象准确性”的表述核对后保留，没有按常识擅自改成另一种对应。

## 验证方法与边界

在GitHub Actions执行资产校验、原有工程测试与新增资产检查、内部构建、check-build、两次构建的ZIP哈希一致性、Python编译及Git空白检查。测试矩阵为Python 3.11和3.13；最终提交的实际结果以PR #4关联该head SHA的CI为准，不能把父提交通过自动继承为后续提交通过。

本地执行了PDF字节/页数检查、抽样视觉核对与新增Python文件语法编译；没有把本地语法编译报告为全量工程测试或真实模型行为回归。

维护索引JSON、接入记录、候选目录、测试与评测预期不进入运行包。运行时回查作者、PDF哈希及页码，使用已打包的XP-T-005阅读记录头部和各锚点，不依赖维护索引JSON。候选流程需要真实行为验收时，由隔离评测宿主显式挂载候选文件，不能为运行测试而直接正式启用。

仍未完成：全书逐字校对、独立人工语义复核、真实宿主/模型完整行为回归、课程及书籍再分发授权。前三个历史来源仍只有文件级定位。rights_status保持 `internal_only_pending_confirmation`；公共发布和严格证据门禁不绕过。

## 回退与审批

当前仅提交分支和草稿PR，不合并main，不对外发布。完整接入回退可回到基线 `22cc95abc810230c4c2aa8a12ea9d6b7c500a045`，或按PR提交执行Git revert；不要从dist反向覆盖源文件。用户审阅、独立语义复核、真实行为回归与启用审批应分别记录，不能用“PR已提交/已合并”替代。
