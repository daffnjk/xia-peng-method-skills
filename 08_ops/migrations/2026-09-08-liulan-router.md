# 《学习之美》接入记录

用户在全书提炼交付后明确要求“添加这本书”，本轮将其加入已有 Router 的外部参考资源，版本 0.4.0。没有将候选 learning-transfer 注册为独立 Skill，没有改变四份夏鹏课程及其锁，也没有编造 XP-T-005。

唯一接入副本位于 `.agents/skills/xia-peng-method-router/references/`；此前候选保留为审阅历史，不再是 Router 的编辑源。资源包括全书提炼、按需应用及带原 PDF SHA-256 的来源记录。原稿和全文 OCR 未复制入库。新增十五道 Router 评测，XP-E-075—089；原十五道候选题保留在非运行候选目录。

权限：用户授权本地添加，不表示材料可公开再分发。本次不推送、不创建远端公开 PR。使用本地 codex/liulan-learning-candidate 分支和 PR 草案供审阅。

验证结果在执行后补记；真实宿主回归不得由静态检查替代。

## 实际验证

- `assets.py validate`：通过，7 Skills、4 份课程来源、89 道题；另有 3 个 Router 外部参考资源。
- `unittest discover -s tests -v`：79 项全部通过。初次失败源于测试夹具硬编码 Skill 版本；夹具改为读取注册版本，并将历史迁移测试明确固定在其 0.3.0 目标版本。没有改写迁移脚本。
- `assets.py build`、`validate --check-build`：通过，三份参考资源进入内部运行包。
- `assets.py prepare-eval`：已生成隔离运行资产和不含预期的题目输入。
- `git diff --check`：通过。
- 真实宿主模型回归和独立评分：未执行；没有符合本仓库隔离协议的适配器。当前 Codex CLI 的存在不等于该适配器已配置。十五道新增题只是题库覆盖，不能宣称行为通过。

原有 XP-T-001—003 文件级证据警告仍在。书中外部研究未独立核验，扫描文本不是逐字校勘结果。未提交或推送原 PDF、OCR 全文；候选目录仅保留历史记录。当前是已接入本地 Router、待真实行为验收的变更。
