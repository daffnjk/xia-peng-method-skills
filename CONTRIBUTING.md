# 贡献与维护

先阅读 `AGENTS.md` 和 `08_ops/ARCHITECTURE.md`。此仓库含课程材料及第三方辅助资产，参与维护不等于获得对外再分发授权。

唯一维护源：Skill 在 `.agents/skills/`；知识在 `02_knowledge/*.yaml`；题库在 `06_evals/*.jsonl`；公共规则在 `03_agent/METHOD_POLICY.md`。不要手改 `dist/` 或重新建立兼容目录双向同步。

新来源按接入 SOP 保留原稿、登记版本、保守校对、审核候选和处理冲突。增加来源或更新校对稿后，由审阅者核对 `01_source/source_locks.json`；不得为了绕过失败而盲目重算锁。原文归属与现实效果是不同维度，不能用“来源明确表达”证明方法有效。

修改后执行 README 中的校验、单元测试、构建及构建核对。引入新字段或枚举时同步更新 Schema 和测试。对方法行为有影响时新增相关 JSONL 题目并进行同版本真实宿主回归；提交实际报告，不提交个人资料或编造评分。

PR 必须说明来源和行为影响、迁移方式、已执行检查、未执行验证及回退方式。原始课程内容不静默覆盖；发现冲突同时保留条件与历史。发布需单独通过授权、证据、行为与人工审批门禁。
